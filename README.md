# Bara Gourmet Aceh TypeScript Project

This is a simple TypeScript project that demonstrates a basic "Bara Gourmet Aceh" application.

## Project Structure

```
s-a-ts
├── src
│   ├── index.ts
│   └── types
│       └── index.ts
├── package.json
├── tsconfig.json
└── README.md
```

## Getting Started

To get started with this project, follow these steps:

1. **Clone the repository** (if applicable):
   ```bash
   git clone <repository-url>
   cd s-a-ts
   ```

2. **Install dependencies**:
   Make sure you have Node.js and npm installed. Then run:
   ```bash
   npm install
   ```

3. **Compile TypeScript**:
   To compile the TypeScript files, run:
   ```bash
   npx tsc
   ```

4. **Run the application**:
   After compiling, you can run the application using Node.js:
   ```bash
   node dist/index.js
   ```

## Usage

When you run the application, it will log "Hello, Bara Gourmet Aceh in Here" to the console.

## License

This project is licensed under the MIT License.